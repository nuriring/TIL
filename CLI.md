## CLI

# GUI VS CLI

1. GUI(그래픽 유저 인터페이스)
   1. 그래픽을 통해서 사용자와 컴퓨터가 상호 작용하는 방식
2. CLI(커맨드 라인 인터페이스)
    	1. 터미널을 통해 사용자와 컴퓨터가 상호 작용 하는 방식



# CLI 사용 이유

new 라는 폴더를 만들고자 한다면...

 	1. GUI 에서는? : 우클릭 -> 새로만들기 -> 폴더 -> NEW
 	2. CLI에서는? : mkdir 이라는 명령어를 입력하면 바탕화면에 폴더가 만들어짐 mv는 파일을 폴더로 이동시켜줌

3. GIT Bash는 유닉스 계열 운영체제 프로그램 명령어를 사용한다.



## 작업 위치(경로)

### 루트 디렉토리(`/`)

- bash 창을 그냥 켰을 때 windows의 경우 일반적으로 `c드라이브`를 의미



### 홈 디렉토리(`~`)

- ~/Desktop : 홈디렉토리에 데스크탑

- TIL 폴더안에서 BASH HERE 하면 ~/DESKTOP/TIL



### 상대경로

- `./`:현재 작업 하고 있는 폴더를 의미합니다.
- `../`: 현재 작업 하고 있는 폴더의 상위 폴더

TIP. `ctrl + 1` 화면 정리 



## 명령어

`cd` `폴더명`  폴더와 디렉토리는 유사

- 현재 작업중인 디렉토리를 변경하는 명령어

- change directory

  ```bash
  $ cd
  -> 홈 디렉토리로 이동
  
  $ cd folder #tab으로 자동완성
  
  $ cd ..
  ```

  `ls`

  - list segments
  - 현재 작업중인 디렉토리의 폴더/파일 명을 보여주는 명령어
  - 지금 내가 작업하고 있는 위치가 맞는지 확인할 때

```bash
# 기본사용
$ ls
# all 
$ ls -a
#long
$ long

$ ls -a -l
```



`mkdir`

- make directory
- 폴더를 생성하는 명령어
- 폴더 이름 사이에 공백을 넣고 싶다면 따옴표로 묶어서 입력한다.

```bash
$ mkdir folder folder1
$ mkdir "2022년 1월 12일"
```

`touch`

- 파일을 생성하는 명령어
- 폴더 생성과 생성 방법 동일
- 숨김 파일 `.`을 파일명 앞에 붙입니다.

``` bash
$ touch test.txt
```



`start` `open`

- 폴더/파일을 여는 명령어

`rm`

- remove
- GUI는 휴지통으로 보낸다. 
- CLI는 `완전 삭제`
- `-r` : recrusive 옵션

```bash
$ rm test.txt
$ rm -r folder/
```



`mv`

- move

- 위치 이동할 폴더가 없다면 파일명 변경으로 작동 

  ```bash
  $ mv {이동시킬 파일/폴더} {이동할 폴더}
  $ mv {변경시킬 파일/폴더} {변경할 이름}
  ```

  

cd와 ls 명령어 기억



## why GIT & GITHUB

내가 쓰는 프로그램을 왜 쓰는지 

### 1. GIT을 이용한 버전 관리

GIT : 분산 버전 관리 프로그램

-> 백업, 복구, 협업이 가능



GITHUB를 이용한 포트폴리오

-> 코드개발에 대한 관심도를 나타냄

레포지토리 -> 프로젝트 단위 가상의 저장소

STAR -> 사람들이 내 코드를 즐겨찾기

깃허브에 몇번의 버전 관리를 했는지를 알려준다

깃허브 레포지토리에다가 버전을 업로드 할 때마다 칸 하나가 하루 에 몇번 업로드를 했는지 -> 잔디밭

1일 1커밋 -> 주말에도 코딩을 했다. 

거창한 프로젝트? ㄴㄴ TIL 내가 오늘 이걸 공부했다. 

기초가 가장 중요하다 



대부분 기업이 JAVA를 사용하는게 맞음

취업을 할려면 JAVA를 공부해야 하나?

PYTHON 배운다고 취업 안되나? 반은 맞고 반은 틀림

WHY? 적어도 이 프로그래밍 언어를 어떻게 학습을 했고 이 학습 코스를 내가 증명할 수 있다면 JAVA로 언어전환하는거 어렵지 않음



TIL -> Today I learned

GITHUB에다가 TIL 열심히 작성하기 

싸피라는 내용만쏙 빼고 TIL에 내용 열심히 정리하기

GITHUB 과 GIT은 다르다



